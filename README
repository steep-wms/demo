start the Steep webserver, e.g. docker, with (use absolute paths instead of "./" !)
docker run --name steep -d --rm -p 8080:8080 --user 0 \
 -v /var/run/docker.sock:/var/run/docker.sock
 -v /<PATH-TO-TILES>/:/tmp/tiles:ro \
 -v /tmp/:/tmp/ \
 -v ./steep/conf/steep.yaml:/steep/conf/steep.yaml \
 -v ./steep/conf/services/demo.yaml:/steep/conf/services/demo.yaml \
 steep/steep:5.6.0

start the webserver in /src with
npm run dev